ad_pkgs_update: false

ad_realm: DOMAIN.LOCAL
ad_domain: domain.local
ad_ou: OU=Computers,DC=domain,DC=local

ad_admin_username: Administrator
ad_admin_password: verysecurepassword

ad_other_domain_to_realm: []
  # - domain.corp

ad_permit_groups: []
#   - ""

ad_default_shell: "/usr/bin/bash"
ad_home_dir: "/home/%u"

ad_replace_sss_conf: false
ad_reboot_if_require: true

ad_config_ssh: true
ad_ssh_display_motd: false
ad_ssh_banner: false
banner_header: |
  ###############################################################
  #                  This is a private server!                  #
  #       All connections are monitored and recorded.           #
  #  Disconnect IMMEDIATELY if you are not an authorized user!  #
  ###############################################################
